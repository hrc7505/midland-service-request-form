/**
 * Defines the validation props to be applied to Fluent UI `Field` and `Input`
 * components, including validation states and messages.
 */
export default interface IFieldValidation {
    fieldProps?: Record<string, unknown>;
    inputProps?: Record<string, unknown>;
}