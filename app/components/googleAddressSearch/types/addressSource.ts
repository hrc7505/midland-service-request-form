/**
 * Indicates how the address was populated:
 * - google: via Google Places autocomplete
 * - autofill: via browser autofill
 * - manual: user entered manually
 */
export type AddressSource = "google" | "autofill" | "manual";
